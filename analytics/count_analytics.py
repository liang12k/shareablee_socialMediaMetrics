# # TODO -- need to generalize the naming to handle numerous other media sources

class UserCounts(object):
    def getAllFavorites(user_id, startDate, endDate):
        return
    
    def getAllRetweets(user_id, startDate, endDate):
        return
    
    def getAllReplies(user_id, startDate, endDate):
        return
    
    def getAllTweets(user_id, startDate, endDate):
        return